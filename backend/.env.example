# Backend environment variables example (dotnet maps __ to nested keys)
# MongoDB
Mongo__ConnectionString=mongodb://mongo:27017
Mongo__Database=factures_DB

# SMTP settings
SmtpSettings__Host=smtp.example.com
SmtpSettings__Port=587
SmtpSettings__User=your@domain.com
SmtpSettings__Pass=your-smtp-password
SmtpSettings__From=your@domain.com

# Automatización (minutos)
TiempoEsperaDesactivacionMin=1
IntervaloAutomatizacionMin=1

# En producción, usar secretos y no guardar credenciales en repositorios.
